synchroniezd的底层实现
jdk早期是重量级锁
改进 ---》有了锁升级的概念：《我是厕所所长》
第一个访问某把锁的线程，来了之后先在锁对象想markword记录这个线程，（如果只有这一个线程访问，实际上是没有给这个对象加锁的，在
内部实现的时候，只是记录了这个线程的ID，）---偏向锁

偏向锁如果有线程争用，升级为自旋锁

--自旋锁转圈十次后，升级为重量级锁，重量级锁就去操作系统申请资源

执行时间断(加锁代码)，线程少，用自旋锁（自动的）
执行时间长，线程多，用系统锁


